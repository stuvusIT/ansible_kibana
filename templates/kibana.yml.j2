# Kibana is served by a back end server. This setting specifies the port to use.
server.port: {{ kibana_server_port }}

# Specifies the address to which the Kibana server will bind. IP addresses and host names are both valid values.
# The default is 'localhost', which usually means remote machines will not be able to connect.
# To allow connections from remote users, set this parameter to a non-loopback address.
server.host: {{ kibana_server_host }}

# Enables you to specify a path to mount Kibana at if you are running behind a proxy. This only affects
# the URLs generated by Kibana, your proxy is expected to remove the basePath value before forwarding requests
# to Kibana. This setting cannot end in a slash.
{% if kibana_server_basepath is defined %}
server.basePath: "{{ kibana_server_basepath }}"
{% endif %}

# The maximum payload size in bytes for incoming server requests.
{% if kibana_server_maxPayloadBytes is defined %}
server.maxPayloadBytes: "{{ kibana_server_maxPayloadBytes }}"
{% endif %}

# The Kibana server's name.  This is used for display purposes.
#server.name: "your-hostname"
{% if kibana_server_name is defined %}
server.name: "{{ kibana_server_name }}"
{% endif %}

# The URL of the Elasticsearch instance to use for all your queries.
elasticsearch.url: {{ kibana_elasticsearch_url | mandatory }}

# When this setting's value is true Kibana uses the hostname specified in the server.host
# setting. When the value of this setting is false, Kibana uses the hostname of the host
# that connects to this Kibana instance.
{% if kibana_elasticsearch_preserveHost is defined %}
elasticsearch.preserveHost: "{{ kibana_elasticsearch_preserveHost }}"
{% endif %}

# Kibana uses an index in Elasticsearch to store saved searches, visualizations and
# dashboards. Kibana creates a new index if the index doesn't already exist.
{% if kibana_kibana_index is defined %}
kibana.index: "{{ kibana_kibana_index }}"
{% endif %}

# The default application to load.
{% if kibana_kibana_defaultAppID is defined %}
kibana.defaultAppId: "{{ kibana_kibana_defaultAppID }}"
{% endif %}

# If your Elasticsearch is protected with basic authentication, these settings provide
# the username and password that the Kibana server uses to perform maintenance on the Kibana
# index at startup. Your Kibana users still need to authenticate with Elasticsearch, which
# is proxied through the Kibana server.
{% if kibana_elasticsearch_username is defined %}
elasticsearch.username: "{{ kibana_elasticsearch_username }}"
elasticsearch.password: "{{ kibana_elasticsearch_password | mandatory }}"
{% endif %}

# Enables SSL and paths to the PEM-format SSL certificate and SSL key files, respectively.
# These settings enable SSL for outgoing requests from the Kibana server to the browser.
#server.ssl.enabled: false
#server.ssl.certificate: /path/to/your/server.crt
#server.ssl.key: /path/to/your/server.key
{% if kibana_server_ssl_enabled is defined and kibana_server_ssl_enabled %}
server.ssl.enabled: {{ kibana_server_ssl_enabled }}
server.ssl.cert: "{{ kibana_server_ssl_certificate | mandatory }}"
server.ssl.key: "{{ kibana_server_ssl_key | mandatory }}"
{% endif %}

# Optional settings that provide the paths to the PEM-format SSL certificate and key files.
# These files validate that your Elasticsearch backend uses the same key files.
#elasticsearch.ssl.certificate: /path/to/your/client.crt
#elasticsearch.ssl.key: /path/to/your/client.key
{% if kibana_elasticsearch_ssl_certificate is defined %}
elasticsearch.ssl.certificate: "{{ kibana_elasticsearch_ssl_certificate | mandatory }}"
elasticsearch.ssl.key: "{{ kibana_elasticsearch_ssl_key | mandatory }}"
{% endif %}


# Optional setting that enables you to specify a path to the PEM file for the certificate
# authority for your Elasticsearch instance.
#elasticsearch.ssl.certificateAuthorities:
{% if kibana_elasticsearch_ssl_certificateAuthorities is defined %}
elasticsearch.ssl.certificateAuthorities: {{ kibana_elasticsearch_ssl_certificateAuthorities }}
{% endif %}

# To disregard the validity of SSL certificates, change this setting's value to 'none'.
#elasticsearch.ssl.verificationMode: full
{% if kibana_elasticsearch_ssl_verificationMode is defined %}
elasticsearch.ssl.verificationMode: "{{ kibana_elasticsearch_ssl_verificationMode }}"
{% endif %}

# Time in milliseconds to wait for Elasticsearch to respond to pings. Defaults to the value of
# the elasticsearch.requestTimeout setting.
#elasticsearch.pingTimeout: 1500
{% if kibana_elasticsearch_pingTimeout is defined %}
elasticsearch.pingTimeout: "{{ kibana_elasticsearch_pingTimeout }}"
{% endif %}

# Time in milliseconds to wait for responses from the back end or Elasticsearch. This value
# must be a positive integer.
#elasticsearch.requestTimeout: 30000
{% if kibana_elasticsearch_requestTimeout is defined %}
elasticsearch.requestTimeout: "{{ kibana_elasticsearch_requestTimeout }}"
{% endif %}

# List of Kibana client-side headers to send to Elasticsearch. To send *no* client-side
# headers, set this value to [] (an empty list).
#elasticsearch.requestHeadersWhitelist: [ authorization ]
{% if kibana_elasticsearch_requestHeadersWhitelist is defined %}
elasticsearch.requestHeadersWhitelist: {{ kibana_elasticsearch_requestHeadersWhitelist }}
{% endif %}

# Header names and values that are sent to Elasticsearch. Any custom headers cannot be overwritten
# by client-side headers, regardless of the elasticsearch.requestHeadersWhitelist configuration.
#elasticsearch.customHeaders: {}
{% if kibana_elasticsearch_customHeaders is defined %}
elasticsearch.customHeaders: {{ kibana_elasticsearch_customHeaders }}
{% endif %}

# Time in milliseconds for Elasticsearch to wait for responses from shards. Set to 0 to disable.
#elasticsearch.shardTimeout: 0
{% if kibana_elasticsearch_shardTimeout is defined %}
elasticsearch.shardTimeout: {{ kibana_elasticsearch_shardTimeout }}
{% endif %}

# Time in milliseconds to wait for Elasticsearch at Kibana startup before retrying.
#elasticsearch.startupTimeout: 5000
{% if kibana_elasticsearch_startupTimeout is defined %}
elasticsearch.startupTimeout: {{ kibana_elasticsearch_startupTimeout }}
{% endif %}

# Specifies the path where Kibana creates the process ID file.
#pid.file: /var/run/kibana.pid
{% if kibana_pid_file is defined %}
pid.file: {{ kibana_pid_file }}
{% endif %}

# Enables you specify a file where Kibana stores log output.
#logging.dest: stdout
{% if kibana_logging_dest is defined %}
logging.dest: {{ kibana_logging_dest }}
{% endif %}

# Set the value of this setting to true to suppress all logging output.
#logging.silent: false
{% if kibana_logging_silent is defined %}
logging.silent: {{ kibana_logging_silent }}
{% endif %}

# Set the value of this setting to true to suppress all logging output other than error messages.
#logging.quiet: false
{% if kibana_logging_quiet is defined %}
logging.quiet: {{ kibana_logging_quiet }}
{% endif %}

# Set the value of this setting to true to log all events, including system usage information
# and all requests.
#logging.verbose: false
{% if kibana_logging_verbose is defined %}
logging.verbose: {{ kibana_logging_verbose }}
{% endif %}

# Set the interval in milliseconds to sample system and process performance
# metrics. Minimum is 100ms. Defaults to 5000.
#ops.interval: 5000
{% if kibana_ops_interval is defined %}
ops.interval: {{ kibana_ops_interval }}
{% endif %}

# The default locale. This locale can be used in certain circumstances to substitute any missing
# translations.
#i18n.defaultLocale: "en"
{% if kibana_i18n_defaultLocale is defined %}
i18n.defaultLocale: {{ kibana_i18n_defaultLocale }}
{% endif %}
